{
  "name": "@codex-remote/adapters",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "node --test --import tsx test/**/*.test.ts"
  }
}
